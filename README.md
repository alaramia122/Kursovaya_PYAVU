# Kursovaya_PYAVU
# Анализатор частоты слов в тексте через префиксное дерево
# топ-K  
**Учебный проект по дисциплине ПЯВУ**  
**Автор:** Осипенко Гавриил Васильевич  
**Группа:** М7О-407С-22  
**Год:** 2025  

---
### О проекте
Программа выполняет частотный анализ русских текстов с использованием **префиксного дерева** и выводит **Топ-K** самых частых слов.

**Особенности:**
- Полная поддержка русского языка (кириллица)
- Три режима сортировки: по частоте, по алфавиту, без сортировки
- Анализ одного файла или всей папки
- Красивый консольный интерфейс с рамками
- Автоматическое сохранение результатов в папку `Records`
  
- **Стандарт языка C++17**

---
### Требования к текстовым файлам
- Кодировка: **ANSI (Windows-1251)** — обязательно!
- Расположение: папка `SourceTexts` рядом с `.exe`
- Формат: `.txt`

> Внимание: UTF-8 не поддерживается — будет некорректное чтение русских букв.

---
### Как запустить
1. Скачайте релиз или скопируйте и соберите репозиторий в Visual Studio 2022+. Ветка Release.
2. Поместите текстовые файлы в папку `SourceTexts`
3. Запустите `.exe`
4. Следуйте подсказкам на экране

---
### Инструкция по сборке
!!! Проект использует Windows-специфичные функции:
- `<conio.h>` (`_getch()`, `_kbhit()`)
- `<Windows.h>` (`SetConsoleCP`, `SetConsoleOutputCP`)
- `system("cls")` и `system("color")`

Сборка под **Visual Studio 2022+**:
1. Клонируйте репозиторий в Visual Studio
2. Установите локальный отладчик в режим Release.
3. Запустите локальный отладчик.

При последующих запусках можно использовать .exe файл. Находится в Kursovaya_PYAVU\x64\Release.

---
### Структура проекта
|── SourceTexts/                ← сюда кладёте .txt файлы (ANSI!)

├── TRPO_Records/               ← сюда сохраняются результаты

├── Header.h                    ← центральный заголовок

├── Struct.h                    ← структуры данных

├── trie.h / trie.cpp           ← префиксное дерево (сердце проекта)

├── file_reader.h / .cpp        ← чтение и нормализация слов

├── menu.h / menu.cpp           ← весь интерфейс

├── output.h / output.cpp       ← красивый вывод и сохранение

├── utils.h / utils.cpp         ← input_data_reader, contains_only и др.

└── main.cpp                    ← точка входа

---
